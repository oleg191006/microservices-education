services:
    redis:
        image: redis:alpine
        ports:
            - '6379:6379'
        restart: always

    your-service:
        build: .
        depends_on:
            - redis
        environment:
            REDIS_HOST: localhost
